# tools/fixtures

Fixture rounds and player histories used to test `packages/engine` without
a database. Not yet populated — depends on the initial evaluator families
and confirmed Grassy Creek hole data (some fixtures are written against
real holes so they're meaningful, not just structurally valid).
