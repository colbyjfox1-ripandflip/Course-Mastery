// @course-mastery/engine
//
// The pure Mastery evaluation engine. Zero runtime dependency on Supabase,
// Postgres, or the network — see ARCHITECTURE.md §6. Conceptual shape:
//
//   evaluate(round, playerHistory, definitions, config) => EvaluationResult
//
// Intentionally empty scaffold: real evaluator families land in
// Checkpoint 1 of IMPLEMENTATION_PLAN.md, once the initial accomplishment
// catalog (PRODUCT_SPEC.md §5.1) is confirmed and there's real Grassy
// Creek hole data to write fixtures against.

export {};
