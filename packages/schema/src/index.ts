// @course-mastery/schema
//
// Shared Zod schemas: the normalized round contract, course/tee/hole
// reference types, and accomplishment/unlock/ledger fact types. This
// package has zero dependency on Supabase, React Native, or the engine —
// every other package (schema consumers) depends on this one, never the
// reverse.
//
// Intentionally empty scaffold: real schemas land in Checkpoint 1 of
// IMPLEMENTATION_PLAN.md, once Grassy Creek course data and the initial
// accomplishment catalog are confirmed. See PRODUCT_SPEC.md §12 for the
// normalized round contract this package will implement.

export {};
